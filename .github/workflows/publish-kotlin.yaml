name: publish-kotlin

on:
  push:
    tags:
      - '**'

jobs:
  publish:
    uses: JavierSegoviaCordoba/reusable-workflows/.github/workflows/publish-kotlin.yaml@4ec55f3e2975433787c8973b5792a7f6a94a5bcf # main
    with:
      publish-maven-central: true
      #TODO: publish-gradle-plugin-portal: "{{ publish-gradle-plugin-portal }}"
      #TODO: publish-jetbrains-marketplace: "{{ publish-jetbrains-marketplace }}"
      publish-docs: true
      publish-github-release: true
    secrets: inherit
